#include <stdio.h>
int main()
{
    int a,b,c;
    printf("Enter three numbers: ");
    scanf("%d %d %d",&a,&b,&c);
    if
    (a>=b && a>=c)
        printf("%d is the largest number.\n",a);
    else if 
    (a>=b && a<=c)
    printf("%d is the largest number.\n"c;
    else 
    print("%d is the largest number.\n"b);
    return 0;
}
